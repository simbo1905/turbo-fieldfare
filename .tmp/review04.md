# review04 — reject reused benchmark results

Work only on review finding P1 in `benchmark_run.py`. Use Red/Green TDD proving `--results` must be absent or empty and that no previous output can be hashed after a backend failure. Implement fail-closed validation without deleting user data. Use uv only; fake backends only; no model. Do not commit. Report exact files/tests.
