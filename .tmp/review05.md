# review05 — propagate benchmark failures

Work only on review finding P2 in `benchmark_run.py`. Use Red/Green TDD proving all chunk audit records and the run manifest are written, but the command returns nonzero if any backend command fails or required output is absent. Successful fixtures remain zero. Use uv only; no model. Do not commit. Report exact files/tests.
